#include<stdio.h>: 包含標準輸入輸出標頭文件，以便使用 printf 函式。

void main(): 主函式的開始，返回值為 void，表示不返回任何值。

int a, b;: 宣告兩個整數變數 a 和 b。

a = 15; b = 1;: 初始化變數 a 為 15，b 為 1。

printf("%d \n", a | b);: 位元 OR 運算，將 a 和 b 的對應位元進行 OR 運算，然後輸出結果。

printf("%d \n", a & b);: 位元 AND 運算，將 a 和 b 的對應位元進行 AND 運算，然後輸出結果。

printf("%d \n", a ^ b);: 位元 XOR 運算，將 a 和 b 的對應位元進行 XOR 運算，然後輸出結果。

printf("%d \n", a << 1);: 位元左移 1 位，將 a 的二進位表示向左移動一位，然後輸出結果。

printf("%d \n", a >> 1);: 位元右移一位，將 a 的二進位表示向右移動一位，然後輸出結果。

printf("%d \n", ~a);: a 的補數運算，按位 NOT 運算，將 a 的所有位元取反，然後輸出結果。




